@model IEnumerable<DomenicaEscobar_ExamenP1.Models.DomenicaEscobar_Tabla>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DEAtributo1)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DEAtributo2)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DEAtributo3)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DEFecha)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DEAtributo1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DEAtributo2)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DEAtributo3)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DEFecha)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
